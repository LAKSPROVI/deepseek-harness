/** Host loader entry for the browser-only voice-input composer control. */

/**
 * Provides no host-side behavior: transcription is owned by
 * `@deepseek-ai/dsh-voice-input`, which this package reaches as a Remote
 * namespace from the browser.
 */
export function apply(): void {}
